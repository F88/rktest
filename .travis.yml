sudo: required
services:
  - docker

language: ruby

before_install:
    - sudo service mysqld stop
    - sudo chkconfig mysqld off
    - docker-compose -v
#    - docker-compose --file docker-compose.yml up --detach 
    - docker-compose up -d

script:
    - ruby -v
