sudo: required
services:
  - docker

language: ruby

before_install:
    - /etc/init.d/mysql stop
    - chkconfig mysqld off
    - docker-compose -v
#    - docker-compose --file docker-compose.yml up --detach 
    - docker-compose up -d

script:
    - ruby -v
